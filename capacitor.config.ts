import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'frank.hartman.angular-ionic-demo',
  appName: 'Angular-ionic-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
